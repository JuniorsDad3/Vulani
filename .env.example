# Copy this file to ".env" in this same folder and fill in your
# REAL values there. The real ".env" file must NEVER be committed
# to git — it's already excluded in .gitignore.

# The code your admin.html gate checks — change this to something
# private that only your team knows.
ADMIN_ACCESS_CODE=changeme123

# Email settings (only needed if you want confirmation emails —
# leave MAIL_USERNAME/MAIL_PASSWORD blank to skip email entirely,
# the app will still work fine without it)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=itradeafrika@gmail.com
MAIL_PASSWORD=kiie bflv obhd gxxn
ALERT_EMAIL=itradeafrika@gmail.com
